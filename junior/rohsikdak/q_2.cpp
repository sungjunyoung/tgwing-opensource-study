#include<iostream>
using namespace std;
int sumof5(int a, int b);
int num;
int sum;

int main () {
	num = 0;
	if (num < 1000)
	sumof5(num, sum);
	else
	cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof5(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;

	return 0;

}

int sumof5(int a, int b)
{
	b = (b + a);
	a = (a + 5);
	sum = b;
	num = a;
	cout << "Num = " << num << "| Sum = " << sum << endl;

	return 0;
}
