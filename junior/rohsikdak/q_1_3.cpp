#include<iostream>
using namespace std;
int sumof15(int a, int b);
int num;
int sum;

int main (){
	num = 0;
	sum = 0;
	if (num < 1000)
	sumof15(num, sum);
	else
	cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof15(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;


return 0;

}

int sumof15(int a, int b)
{
	b = (b + a);
	a = (a + 15);
	sum = b;
	num = a;
	cout << "Num = " << num << "| Sum = " << sum << endl;

	return 0;
}
