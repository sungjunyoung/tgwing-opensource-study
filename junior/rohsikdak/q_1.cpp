#include<iostream>
using namespace std;
int sumof3 (int a, int b);
int num;
int sum;
int main() {
	
	num = 0;

	sumof3(num, sum);
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;

	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;
	if (num < 1000)
		sumof3(num, sum);
	else
		cout << "Calculate finish : " << sum << endl;


}

int sumof3(int a, int b)
{
	b = (b + a);
	a = (a + 3);
	sum = b;
	num = a;
	cout << "Num = " << num << "| Sum = " << sum << endl;

	return 0;
}
